@model TheLegend.Models.Humor

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Humor</legend>

        @Html.HiddenFor(model => model.HumorId)

        <div class="editor-label">
            @Html.LabelFor(model => model.EstadoHumor)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EstadoHumor)
            @Html.ValidationMessageFor(model => model.EstadoHumor)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
