@model IEnumerable<TheLegend.Models.Humor>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align: right; color: #00BFFF; font-size:x-large">Index </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstadoHumor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoHumor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HumorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HumorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HumorId })
        </td>
    </tr>
}

</table>
