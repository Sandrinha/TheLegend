@model IEnumerable<TheLegend.Models.RelationShip>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.User2.UserName)
        </th>
        <th>
            @Html.DisplayName("Tipe de Relação")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    if(item.UserId1==WebSecurity.GetUserId(User.Identity.Name)){
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User2.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelationShipId })
        </td>
    </tr>
    }
}

</table>
