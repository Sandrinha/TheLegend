@model TheLegend.Models.RelationShip

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>RelationShip</legend>

                <div class="editor-label">
            @Html.LabelFor(model => model.UserId1, "UserId1")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId1", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId1)
            </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId2, "UserId2")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId2", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId2)
        </div>

            <div class="editor-label">
                @Html.LabelFor(model => model.TagRelationId, "Tag")
            </div>
            <div class="editor-field">
                @Html.DropDownList("TagRelationId", String.Empty)
                @Html.ValidationMessageFor(model => model.TagRelationId)
            </div>

            <p>
                <input type="submit" value="Create" />
            </p>
</fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
