@model IEnumerable<TheLegend.Models.Ask>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <p>
            Os meus Pedidos Por Tratar
        </p>
    </tr>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserAskId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserDestinId)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.UserOriginId == WebSecurity.GetUserId(User.Identity.Name))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserAskId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserDestinId)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.AskId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.AskId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.AskId })
                </td>
            </tr>
        }
    }

</table>

<table>
    <tr>
        <p>
            Pedidos Por Tratar
        </p>
    </tr>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserOriginId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserDestinId)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.UserAskId == WebSecurity.GetUserId(User.Identity.Name))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserOriginId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserDestinId)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.AskId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.AskId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.AskId }) |
                    @Html.ActionLink("Responder", "Responder", new { id = item.AskId })
                </td>
            </tr>
        }
    }

</table>