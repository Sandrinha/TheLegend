@model IEnumerable<TheLegend.Models.TagRelation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Force)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Force)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TagRelationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TagRelationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TagRelationId })
        </td>
    </tr>
}

</table>
